# План доработки проекта

Рабочий документ. Отмечай выполненное чекбоксами по ходу.

---

## 1. Контекст

Проект получил ревью тестового задания. Часть замечаний — прямые пробелы
(нет axios, socket.io не подключён, нет i18n, нет форм, нет 404, нет кастомных хуков),
часть — требования расширенного ТЗ, которые не были реализованы вовсе (SSR, JWT, тесты,
PWA, чарты и т.д.).

### Что ревьюер оценил положительно (не ломать)

- Структура: страницы и компоненты раздельно, мок отдельно, стор отдельно.
- Декомпозиция страниц.
- Redux Toolkit, разбитый на два слайса; реактивный выбор прихода, корректные счётчики.
- Переиспользуемые утилиты `formatDate` и `getOrderStats`.
- Глобальный layout, оборачивающий приложение.
- Удаление через модалку подтверждения.
- `setInterval` для часов с корректной отпиской.

### Замечания ревьюера

| Замечание                                | Фаза      |
| ---------------------------------------- | --------- |
| Нет axios                                | 1.3       |
| socket.io установлен, но подключения нет | 1.2 + 1.3 |
| Нет i18n                                 | 1.5       |
| Нет работы с формами                     | 1.4       |
| Нет обработки 404                        | 1.3       |
| Кастомные хуки не писал                  | 1.3       |
| Типизация — интерфейсы, не более         | 1.1       |
| Нет меню слева, по стилям средне         | 1.4       |

### Пункты расширенного ТЗ

TypeScript, SSR (Next.js), unit-тесты, i18n, JWT, Web Storage, Lazy Loading,
Charts, GraphQL, Web Workers, PWA, Event-Driven Architecture, FrontEnd Optimization,
Task Runners (npm scripts), интеграционные и функциональные тесты.

**Исключены из скоупа по решению от 2026-08-01:** Microservices,
Micro Frontend Architecture, WebAssembly.

---

## 2. Принятые решения

| Вопрос         | Решение                                                                        | Обоснование                                                                                                                                                      |
| -------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Архитектура    | Фронт (Next) и API (NestJS) — **два отдельных репозитория**                    | API может быть передан другой команде; разделять дешевле сейчас, чем потом                                                                                       |
| Фронт          | **Одно** Next-приложение, без монорепо и Turborepo                             | Micro Frontend снят → делить нечего; оркестрация обычными npm-скриптами (пункт ТЗ «Task Runners»)                                                                |
| Бэкенд         | NestJS                                                                         | Из коробки модули, guards под JWT, `@nestjs/graphql`, `@nestjs/websockets`. Альтернатива — Express, экономит ~1 день, но всё пишется руками                      |
| Стейт-менеджер | Остаёмся на **Redux Toolkit**                                                  | Ревьюер стор уже одобрил; `listenerMiddleware` закрывает EDA, `createAsyncThunk` — состояния загрузки. Zustand не даёт выигрыша и добавляет риск новых замечаний |
| Хранилище API  | **In-memory + абстракция репозитория**, Postgres — опциональная последняя фаза | Микросервисов нет, инстанс один, БД в ТЗ не требуется. Ревьюеру не нужно поднимать Postgres. Переезд = один новый класс                                          |
| Контракт FE↔BE | GraphQL-схема + OpenAPI, типы через `graphql-codegen` / `openapi-typescript`   | Не нужен общий npm-пакет типов — главная боль поли-репо                                                                                                          |
| socket.io      | Счётчик активных сессий                                                        |                                                                                                                                                                  |
| Формы          | **Formik + Yup**                                                               |                                                                                                                                                                  |
| Стили          | Bootstrap + левый сайдбар                                                      | Без миграции на Tailwind                                                                                                                                         |
| i18n           | `next-intl`, локали **RU / EN / UK**, выбор через cookie                       |                                                                                                                                                                  |
| Тесты          | **Vitest + RTL + MSW + Playwright**                                            |                                                                                                                                                                  |
| Порядок работ  | Сначала блок 1 (фидбек), затем блок 2 (ТЗ)                                     | Предъявляемый результат к концу первой недели                                                                                                                    |

### Отклонённые варианты

- **Module Federation** для микрофронтендов — `@module-federation/nextjs-mf` официально
  не поддерживает Next 15 + Turbopack, потребовался бы откат версии Next.
  (Пункт снят из скоупа целиком.)
- **WASM на Rust** — тянет Rust-тулчейн в Docker-образ. (Пункт снят.)
- **`next-pwa`** — проект заброшен, берём Serwist.
- **RTK Query** — увёл бы axios на второй план, а ревьюер требует его явно.

---

## 3. Текущее состояние кода

Актуально после фазы 1.1. Что нужно держать в голове, приступая к работам:

- **SSR отсутствует полностью.** `src/app/orders/page.tsx` и
  `src/app/products/page.tsx` начинаются с `"use client"`, данные грузятся
  в `useEffect`. App Router есть, серверного рендеринга данных нет.
- **Загрузка через `useState(true)`** без состояния ошибки — обе страницы.
- **`photo` в типе `Product` — обычная строка**, `next/image` не используется.
- **Хардкод локали:** `"ru-RU"` в `src/components/topMenu/index.tsx`,
  `"Все типы"` в `src/components/select/index.tsx`, тексты модалки и карточек.
- **`socket.io-client` в зависимостях**, но ни одного импорта в коде —
  подключение к серверу появится в фазе 1.3.
- **Слой запросов готов, но не подключён.** `src/services/http.ts` (транспорт)
  и `src/services/api/*` (шесть типизированных методов с разбором ответа) уже
  ходят в API, однако страницы по-прежнему импортируют `src/mocks` напрямую:
  переключение произойдёт вместе с `createAsyncThunk` — следующий пункт
  фазы 1.3. До тех пор `src/mocks` остаётся живым.
- **Адрес API задаётся окружением** (`NEXT_PUBLIC_API_URL`, для серверной
  стороны — `API_URL_INTERNAL`), см. `.env.example`. Без файла берутся
  дефолты — `http://localhost:4000`.

Закрыто в фазе 1.1: JS-файлы, разъезд `id` между `string` и `number`, мок как
JS-модуль, отсутствие линтера и форматтера.
Закрыто в фазе 1.2: появился API-репозиторий — `orders`, `products`, каскадное
удаление, socket.io со счётчиком сессий и доменными событиями, CORS, валидация,
Docker. Данные спрятаны за порт `Repository`, так что переезд на Postgres
(опциональная фаза 2.8) — это новый класс и строка в модуле. Заодно из
зависимостей фронта ушёл серверный `socket.io`.

**Структура выровнена:** приложение больше не лежит в подпапке `my-app/` —
git-корень совпадает с корнем приложения, npm и docker запускаются из одного
места, husky работает без `cd`.

---

## 4. Целевая структура

**Репозиторий 1 — фронтенд (текущий):**

```
src/
├─ app/
│  ├─ layout.tsx              глобальный layout: sidebar + topbar
│  ├─ not-found.tsx           404
│  ├─ error.tsx               обработка ошибок
│  ├─ loading.tsx             skeleton
│  ├─ login/                  форма логина (Formik + Yup)
│  ├─ orders/
│  ├─ products/
│  └─ dashboard/              чарты
├─ components/                презентационные компоненты
│  ├─ layout/sidebar/
│  ├─ layout/topbar/
│  └─ forms/                  OrderForm, ProductForm, LoginForm
├─ features/                  слайсы + селекторы + thunks
├─ hooks/                     кастомные хуки
├─ services/                  axios-инстанс, api-методы, socket-клиент
│  └─ generated/              типы из graphql-codegen / openapi
├─ workers/                   Web Workers
├─ i18n/                      конфиг next-intl
├─ messages/                  ru.json, en.json, uk.json
├─ types/
└─ utils/
```

**Репозиторий 2 — API (NestJS):**

```
src/
├─ auth/         login, refresh, JwtAuthGuard
├─ orders/       controller, service, repository (interface + in-memory)
├─ products/     то же
├─ events/       socket.io gateway, шина доменных событий
├─ graphql/      code-first схема
└─ seed/         данные из старого base/app.js
```

---

## БЛОК 1 — фидбек ревьюера (~5.5 дней)

Цель: закрыть все прежние претензии ревьюера и получить предъявляемую версию.

### Фаза 1.1 — TypeScript-чистка `0.5 дн` — ✅ сделано

Закрывает: «типизация — интерфейсы, не более», остатки JS.

- [x] `components/select/index.js` → `index.tsx` с типизированными пропсами
- [x] `components/cardPlaceholder/index.js` → `index.tsx`
- [x] `base/app.js` вынесен в `src/mocks/seed.json` + `src/mocks/index.ts`
- [x] `enum Currency`, `enum ProductCondition` вместо магических значений
- [x] Дженерики: `ApiResponse<T>`, `Paginated<T>` (`types/api.ts`)
- [x] DTO через утилити-типы: `CreateOrderDto = Omit<Order, "id">` и остальные
- [x] Дискриминированные юнионы для socket-событий (`types/socket.ts`)
- [x] Type guards: `isOrder`, `isProduct`, `isPrice`, `isCurrency`, `isApiError`
- [x] Branded types: `OrderId`, `ProductId` вместо голого `number`
- [x] Убраны все `String()` / `Number()` конвертации идентификаторов
- [x] Строгий TS: `noUncheckedIndexedAccess`, `noImplicitOverride`,
      `noFallthroughCasesInSwitch`, `forceConsistentCasingInFileNames`, `allowJs: false`
- [x] ESLint (flat config) + Prettier + husky + lint-staged
- [x] npm-скрипты: `lint`, `lint:fix`, `format`, `format:check`, `typecheck`, `verify`

#### Отклонение от плана

**`enum ProductType` не сделан намеренно.** В данных 11 категорий (`Мониторы`,
`Периферия`, `Ноутбуки`, …), и это открытый каталог, который ведёт бэкенд.
Enum пришлось бы править при каждой новой категории и он всё равно врал бы про
данные с сервера. Оставлена `type ProductType = string` с константой
`ALL_PRODUCT_TYPES` для фильтра. Глубина типизации добрана в других местах:
branded types, мапленный тип `ServerToClientEvents`, размеченные объединения,
утилити-типы DTO, `assertNever`.

#### Попутно найдено и исправлено

- **Нарушение правил хуков** в `productsCard`: ранний `return` при `product === undefined`
  стоял выше `useState` и `useAppSelector` — при появлении продукта менялся порядок
  хуков. Проп сделан обязательным, ранняя ветка убрана.
- **`ModalWindow` мог получить `id === null`** из `orderProductsCard` — модалка
  рендерилась вне проверки `selectedOrderId`. Добавлена защита.
- **`onClick` висел на `<i>` внутри `<button>`** в `productsCard` и `orderProductsCard` —
  клик по кнопке мимо иконки не срабатывал. Перенесён на `<button>`.
- **Дублирование данных в моке:** у каждого заказа было вложенное поле `products`,
  посимвольно совпадающее с плоским списком. Из сида убрано.
- **Нестрогое сравнение** `selectedOrderId == orderId` в `orderCard` → `===`
  (правило `eqeqeq` теперь ловит такое).

#### Найдено, отложено осознанно

- **`clsx("flex-grow-1", { orders: … })` в `orders/page.tsx`** подставляет литеральную
  строку `orders`, а не хешированный класс CSS-модуля, поэтому `.orders { width: 30% }`
  из `orders/index.module.css` никогда не применялся. Починка меняет раскладку —
  разбирается в фазе 1.4 вместе с сайдбаром. В коде стоит `TODO(1.4)`.
- **`deleteOrder` и `deleteAllOrderProduct` обязаны диспатчиться парой**, иначе
  остаются продукты-сироты. Пока помечено комментарием; каскад делает API в фазе 1.2.
- **Предупреждение `react-hooks/exhaustive-deps`** в `orders/page.tsx` — уходит
  в фазе 1.3 вместе с заменой `useEffect` на `createAsyncThunk`.

#### Проверка

`npm run verify` (typecheck + lint + format:check) и `npm run build` проходят;
остаётся одно известное предупреждение `exhaustive-deps`.

### Фаза 1.2 — минимальный API `1.5 дн` — ✅ сделано

Нужен до axios: без реального сервера подключение остаётся бутафорией.

Репозиторий: `g:/Projects/code/dzencode/berkun-dzencode-api`, NestJS 11, порт 4000.

- [x] Инициализировать репозиторий NestJS
- [x] Модуль `orders`: `GET /orders`, `POST /orders`, `DELETE /orders/:id`
- [x] Модуль `products`: `GET /products`, `POST /products`, `DELETE /products/:id`
- [x] Порт `Repository<TEntity, TDraft, TId>` + in-memory реализации
      `InMemoryOrdersRepository` / `InMemoryProductsRepository`, сид из старого мока
- [x] Каскад: удаление прихода удаляет его продукты
- [x] Модуль `events`: socket.io gateway, счётчик подключённых сессий,
      эмит `sessions:count` на connect/disconnect, ответ на `session:ping`
- [x] Доменные события `order.deleted` / `product.deleted` через EventEmitter2 → broadcast
      (серверная половина EDA)
- [x] CORS, валидация через `ValidationPipe` + class-validator, `.env.example`
- [x] Dockerfile + `docker-compose.yml` в API и общий `docker-compose.full.yml`
      во фронтовом репозитории

#### Отклонение от плана

**Порт хранилища — один обобщённый интерфейс, а не два.** В чеклисте написано
«интерфейсы репозиториев», но у заказов и продуктов контракт совпадает
дословно, поэтому в `domain/repository.ts` объявлен
`Repository<TEntity, TDraft, TId>`, а `OrdersRepository` и `ProductsRepository` —
его псевдонимы с подставленными типами. В пункте 5 они разошлись:
`ProductsRepository` дополнен методом `removeByOrder` — заказам он не нужен,
и в общий порт не попал.

**Внедрение через `Symbol`-токен, а не через абстрактный класс.** Интерфейс
существует только в типах, у Nest-провайдера должна быть runtime-ссылка.
Абстрактный класс дал бы токен без `@Inject`, но перестал бы быть чистым
контрактом — в него легко просачивается реализация. Токены объявлены рядом с
псевдонимами (`ORDERS_REPOSITORY`, `PRODUCTS_REPOSITORY`), выбор реализации
живёт в единственном месте — в `providers` модуля.

**Генерация `id` переехала из сервиса в репозиторий.** Это аналог
автоинкремента: id выдаёт хранилище, потому что только оно знает про уже
существующие записи. Сервис теперь не трогает данные вообще.

**Каскад вызывается напрямую, а не через доменное событие.** Соблазн был:
шина уже есть, `ProductsService` мог бы подписаться на `order.deleted`. Но без
удаления продуктов данные остаются битыми, а обязательный шаг нельзя отдавать
на волю подписчиков — событие рассылает уведомления, и «никто не подписался»
для него нормальная ситуация. `OrdersModule` импортирует `ProductsModule`
и зовёт метод явно; обратной зависимости нет, цикла не возникает.

**При каскаде отдельные `product:deleted` не рассылаются.** Клиент получает
`order:deleted` и убирает продукты заказа одним действием — редьюсер
`deleteAllOrderProduct` во фронтовом сторе уже так и устроен. Десяток событий
подряд на одно действие пользователя только дёргал бы интерфейс.

**Валидация тела запроса переехала с type guard'ов на `ValidationPipe`.** Guards
из `domain/types.ts` остались, но теперь только для сида: `seed.json` — тоже
внешние данные, и разбирать их надо на границе. У HTTP-DTO появились классы
(`CreateOrderDto`, `CreateProductDto`) — `ValidationPipe` работает в рантайме
и читает метаданные декораторов, а интерфейсы после компиляции исчезают.
Поле `order` в DTO объявлено обычным `number`: в брендовый `OrderId` его
превращает контроллер, конвертация живёт на границе.

**`.env` читается через `dotenv`, а не через `@nestjs/config`.** CORS для
socket.io задаётся в декораторе `@WebSocketGateway`, а декораторы вычисляются
при загрузке модуля — до того, как Nest поднимет DI, и `ConfigService` оттуда
недоступен в принципе. `import 'dotenv/config'` первой строкой `main.ts`
наполняет `process.env` раньше загрузки модулей, а `src/config.ts` отдаёт
значения с дефолтами. `@nestjs/config` был установлен и удалён.

**Общий compose — отдельный файл `docker-compose.full.yml` во фронтовом
репозитории.** Он собирает API из соседней папки `../berkun-dzencode-api`, и
если положить это в основной `docker-compose.yml`, то у ревьюера, склонировавшего
только фронт, перестанет подниматься даже фронт. Поэтому обычный
`docker compose up` остался самодостаточным, а обе части поднимаются командой
`docker compose -f docker-compose.full.yml up`.

**`tsconfig` усилен до полного `strict`.** `nest new --strict` включает только
`strictNullChecks`, `noImplicitAny` и `strictBindCallApply`. Добавлены `strict`,
`noUncheckedIndexedAccess`, `noImplicitOverride` — те же настройки, что во
фронтовом репозитории.

**Набор npm-скриптов выровнен с фронтовым:** появились `typecheck`,
`format:check` и `verify`, а `lint` разделён на проверку и `lint:fix`.

#### Попутно найдено и исправлено

- **`findAll()` отдавал ссылку на внутренний массив хранилища.** Любой
  вызывающий код мог отсортировать его на месте или дописать запись мимо
  `create()` — то есть незаметно испортить «базу». С настоящей БД такое
  невозможно, и подмена реализации в фазе 2.8 выявила бы это как расхождение
  поведения. Репозитории теперь отдают копии, а продукты копируют ещё и
  вложенные `guarantee` и `price`.
- **Дефолтный `lint` от NestJS запускается с `--fix`** — то есть «проверка»
  молча переписывает исходники, и в CI такой скрипт бесполезен. Автофикс
  вынесен в отдельный `lint:fix`.
- **Скрипт `format` падал** на шаблоне `"test/**/*.ts"` после удаления
  boilerplate-спеки. Заменён на `prettier --write .` с `.prettierignore`.
- Удалены `app.controller.ts`, `app.service.ts` и обе заготовки тестов —
  они проверяли «Hello World», которого больше нет.
- **Контейнер не стартовал: `EBUSY: resource busy or locked, rmdir '/app/dist'`.**
  В `nest-cli.json` шаблона стоит `deleteOutDir: true`, а `dist` был вынесен в
  именованный том — точку монтирования удалить нельзя. Том убран: `dist` —
  обычный JS без платформенных бинарников, в отличие от `node_modules`, ему
  изоляция от хоста не нужна.
- **Серверный `socket.io` удалён из зависимостей фронта** — он там не
  использовался ни разу, браузеру нужен только `socket.io-client`. Теперь
  сервер живёт в API-репозитории, где ему и место.

#### Найдено, отложено осознанно

- **Счётчик сессий хранится в памяти процесса.** При нескольких инстансах API
  цифры разъедутся: нужен общий адаптер (`@socket.io/redis-adapter`).
  Инстанс сейчас один, вопрос всплывает только вместе с горизонтальным
  масштабированием — это опциональная фаза 2.8.
- **`session:ping` реализован, но клиента у него пока нет** — контракт
  двусторонний, фронт подключится к сокету в фазе 1.3.
- **В API остались jest-скрипты от шаблона.** Тестов нет ни там, ни на фронте;
  стек тестирования выбирается в фазе 2.6.

#### Проверка

`npm run verify` и `npm run build` зелёные в обоих репозиториях; во фронте
остаётся известное предупреждение `exhaustive-deps`.

Проверка шла **против контейнера** (`docker compose up -d`), а не против
локального процесса: DI с `Symbol`-токеном и подписки гейтвея ломаются только
в рантайме, компиляция таких ошибок не ловит.

- HTTP: `GET /orders` → 7, `GET /products` → 34, `POST /orders` → `201`
  с выданным `id`, `DELETE` → `204`, несуществующий id → `404`, нечисловой
  id → `400`.
- Каскад: у заказа 1 было 4 продукта; после `DELETE /orders/1` заказов 6,
  продуктов 30, сирот с `order: 1` — ноль.
- Валидация: отсутствующее поле → `400 ["title should not be empty", …]`,
  лишнее поле → `400 ["property hacked should not exist"]`, битая вложенность →
  `400 ["guarantee.end should not be empty", "price.0.symbol must be one of the
following values: USD, UAH"]`.
- CORS: запрос с `Origin: http://localhost:3000` возвращает
  `Access-Control-Allow-Origin: http://localhost:3000`.
- Сокеты (два клиента, `socket.io-client`): первый получает
  `sessions:count 1`, после подключения второго оба получают `2`,
  `session:ping` возвращает текущее значение, после отключения второго
  первый получает `1`. `DELETE /orders/1` → обоим приходит `order:deleted 1`
  (и ни одного `product:deleted` на каскадные продукты),
  `DELETE /products/5` → обоим `product:deleted 5`.

### Фаза 1.3 — axios, стор, хуки, 404 `1.5 дн` — 🚧 в работе

Закрывает: нет axios, socket не подключён, нет кастомных хуков, нет 404.

- [x] `services/http.ts`: axios-инстанс, `baseURL` из окружения (`apiBaseUrl()`),
      `withCredentials`, таймаут, request-интерсептор как заготовка под 2.2 и
      response-интерсептор, приводящий любую ошибку к классу `HttpError`
- [x] `services/api/orders.ts`, `products.ts` — типизированные методы
      (`getOrders` / `createOrder` / `deleteOrder` и та же тройка для продуктов),
      баррель `services/api/index.ts`, разбор ответа в `services/api/parse.ts`
- [x] **Важно:** вызовы вынесены в модули без `window`, React и Redux —
      проверено рендером серверного компонента, который их зовёт
- [ ] `createAsyncThunk` для загрузки: заменить `useState(loading)` на
      `pending/fulfilled/rejected` в слайсе, появится состояние ошибки
- [ ] `StoreProvider` научить принимать `preloadedState` — заранее, под SSR
- [ ] `services/socket.ts` — клиент socket.io, подключение с переподключением
- [ ] `listenerMiddleware`: socket-события → диспатч в стор (клиентская половина EDA)
- [ ] Кастомные хуки: `useOrders`, `useProducts`, `useSocket`, `useSessionCount`,
      `useClock`, `useLocalStorage`, `useConfirm`, `useOrderStats`
- [ ] `useClock` — перенести туда логику из `topMenu`, сохранив отписку
- [ ] `app/not-found.tsx`, `app/error.tsx`, `app/loading.tsx`
- [ ] `notFound()` при обращении к несуществующему приходу

#### Отклонение от плана

Ниже — записи по мере прохождения пунктов фазы; итог подводится, когда закрыт
последний.

**Пункт 1 (`services/http.ts`).**

**Ошибки нормализуются в класс `HttpError`, а не отдаются как `AxiosError`.**
Слайсы в следующих пунктах кладут ошибку в состояние, а `AxiosError` тянет за
собой `config`, `request` и циклические ссылки — Redux на такое ругается
(non-serializable value). `HttpError` — обычный `Error` с гарантированными
`statusCode` и `message` плюс метод `toApiError()`, отдающий чистый объект под
`rejectWithValue`. `statusCode === 0` означает «ответа не было вовсе» (обрыв
сети, таймаут, отказ CORS) — это единственное состояние, при котором повтор
запроса осмыслен, и для него есть геттер `isNetworkError`.

**Разбор тела ошибки не сводится к `isApiError`.** `ValidationPipe` на бэке
отдаёт `message` массивом строк, обычный `HttpException` — одной строкой, и
существующий guard пропускает только вторую форму. Массив склеивается в строку
отдельной веткой, иначе на любой ошибке валидации формы (фаза 1.4) в
интерфейсе оказалось бы `[object Object]`.

**Базовых адресов два: `NEXT_PUBLIC_API_URL` и `API_URL_INTERNAL`.** Из
браузера API виден по проброшенному наружу порту, из серверного рантайма Next
внутри docker-сети — по имени сервиса. Это ровно тот `TODO(2.1)`, что стоит в
`docker-compose.full.yml`; функция `apiBaseUrl()` выбирает адрес по
`typeof window`, так что в фазе 2.1 останется дописать переменную в compose, а
не править код. Модуль изоморфный: ни `window`, ни `localStorage`, ни React.

**Заведён `.env.example`** — раньше во фронте его не было, а теперь адрес API
задаётся окружением. В `.gitignore` добавлено исключение `!.env.example`:
правило `.env*` прятало и его тоже.

**Пункты 2–3 (`services/api/*`).**

**Ответ сервера разбирается через `parseOrder` / `parseProduct`, а не
приводится к типу через `as`.** Соблазн был большой: `http.get<Order[]>()`
пишется в одну строку и компилятор доволен. Но дженерик axios — это обещание,
а не проверка: он ничего не знает о том, что реально пришло по сети. У нас два
репозитория без атомарных изменений контракта (риск №5 в разделе 5), то есть
разъехавшийся бэк — не гипотеза. Разбор на границе оставляет расхождение в
одном месте с внятным сообщением вместо `undefined` где-то в глубине рендера.
Заодно это ровно то правило, по которому уже разбирается `seed.json`.

**Битое тело ответа — это `HttpError` с настоящим статусом (обычно `200`), а
не с нулём.** Нуль в `HttpError` означает «ответа не было вовсе» и разрешает
повтор запроса; повторять запрос, на который сервер уверенно отвечает мусором,
бессмысленно. Различие пригодится хукам и `error.tsx` в конце фазы. Признак
таких ошибок — `error: "Invalid Response"`.

**У методов свой узкий `RequestOptions`, а не `AxiosRequestConfig`.** Через
полный конфиг вызывающий код мог бы переопределить `baseURL`, `withCredentials`
или интерсепторы, и настройки транспорта расползлись бы по компонентам.
Наружу открыты только `signal` (его даёт `createAsyncThunk`) и `headers` (в
фазе 2.1 туда уйдёт `Cookie` серверного запроса). Побочный эффект — модули
`api/*` не импортируют axios вообще, кроме типа ответа в `parse.ts`.

**Списки возвращаются как `readonly`.** Массив из сети — снимок, а не рабочая
коллекция; сортировать или фильтровать его на месте нельзя, потому что дальше
он уходит в стор. Та же причина, по которой репозитории API отдают копии.

**Барреля `services/index.ts` нет, есть только `services/api/index.ts`.**
Компоненты и thunks должны импортировать `@/services/api`, а `services/http` —
внутренняя кухня транспорта: если в фазе 2.3 чтение переедет на GraphQL,
поменяется реализация этих шести функций, а не места вызова.

#### Попутно найдено и исправлено

**Пункт 1.** Request-интерсептор в первой редакции переприсваивал
`config.baseURL = apiBaseUrl()` на каждом запросе — «чтобы подхватить
окружение». На проверке против живого API выяснилось, что к моменту работы
интерсептора в `config` уже лежит слитое значение, и присваивание затирало
явный `baseURL` конкретного запроса: запрос на заведомо мёртвый порт спокойно
уходил на рабочий и возвращал `200`. Next читает `.env` до загрузки модулей,
так что перечитывать окружение незачем — переприсваивание убрано, интерсептор
остался как точка расширения под фазу 2.2.

#### Найдено, отложено осознанно

**Пункт 1.** Ветка `401 → refresh → retry` не реализована — это фаза 2.2, на
бэке ещё нет `/auth`. В response-интерсепторе стоит `TODO(2.2)` с требованием
дедуплицировать параллельные рефреши; в request-интерсепторе — `TODO(2.2)` на
проброс `Cookie` в серверные запросы.

**Пункты 2–3.** `GET /orders/:id` на бэке нет — есть только список. Значит
пункт «`notFound()` при обращении к несуществующему приходу» решается поиском
по загруженному списку, а не отдельным запросом. Добавлять эндпоинт под один
экран сейчас не стал: это правка в другом репозитории ради случая, который
закрывается фильтром по уже полученным данным.

**Пункты 2–3.** Методы обновления (`PATCH`) не написаны, хотя
`UpdateOrderDto` / `UpdateProductDto` в типах есть: на бэке таких маршрутов
нет, а формы из фазы 1.4 по ТЗ только создают записи. Появится
редактирование — появятся и методы.

**Пункты 2–3.** Пагинация не задействована: `GET /products` отдаёт весь
каталог, и `Paginated<T>` из `types/api.ts` пока не используется никем.
Тридцать четыре записи того не стоят; отметка оставлена комментарием в
`services/api/products.ts`.

#### Проверка

`npm run verify` и `npm run build` зелёные; остаётся известное предупреждение
`exhaustive-deps` в `orders/page.tsx` — оно уйдёт с пунктом про
`createAsyncThunk`.

Пункт 1 проверялся **в рантайме против запущенного API** (`:4000`), а не только
компилятором: `GET /orders` → 200 и 7 записей, `GET /products` → 34 записи;
`baseURL` `http://localhost:4000`, `withCredentials: true`, таймаут 10 с.
Нормализация ошибок — 404 → `{ statusCode: 404, message: "Cannot GET …",
error: "Not Found" }`; 400 от `ValidationPipe` → пять сообщений валидации,
склеенных в одну строку; недоступный порт → `statusCode: 0`,
`isNetworkError: true`, `error: "ECONNREFUSED"`; таймаут → `statusCode: 0`,
`error: "ECONNABORTED"`; произвольный `Error` через `HttpError.from` →
`statusCode: 0`. Отдельно проверено, что `baseURL`, заданный на конкретном
запросе, доходит до сокета — тот самый случай, который ломал первый вариант
интерсептора.

Пункты 2–3 проверялись так же, против живого API, полным циклом:
`getOrders()` → 7 разобранных приходов, `getProducts()` → 34 продукта;
`createOrder()` → приход с выданным сервером `id: 8`; `createProduct()` на этот
приход → продуктов стало 35; `deleteOrder(8)` → приходов снова 7, продуктов 34,
сирот с `order: 8` — ноль (каскад бэка отрабатывает через этот слой);
`createProduct()` + `deleteProduct()` → счётчик вернулся к 34, записи нет.
Ошибки: удаление несуществующего прихода → `HttpError 404 "Заказ 999999 не
найден"`, `createOrder` с пустыми полями → `HttpError 400` со склеенными
сообщениями `ValidationPipe`.

Разбор ответа проверялся отдельно на подставных телах: массив валидных заказов
проходит; объект вместо массива → `HttpError` со `statusCode: 200`,
`error: "Invalid Response"`, сообщением «Ожидался массив, пришло: object»;
битая запись внутри массива и битая сущность → то же самое с текстом от
`parseOrder`. `apiBaseUrl()` в серверном рантайме отдаёт `API_URL_INTERNAL`,
а без него откатывается на публичный адрес.

Изоморфность пункта 3 проверена не рассуждением, а рендером: во временную
страницу `app/smoke-check/page.tsx` (серверный компонент, `force-dynamic`)
добавлен вызов `getOrders()` и `getProducts()`, дев-сервер отдал готовый HTML
с `renderedOn: "server"`, `orders: 7`, `products: 34` — то есть запрос ушёл из
серверного процесса Next, а не из браузера. Страница после проверки удалена.

### Фаза 1.4 — сайдбар и формы `1.5 дн`

Закрывает: «нет меню слева, по стилям средне», «нет работы с формами».

- [ ] Компонент `Sidebar`: сворачиваемый, иконки bootstrap-icons,
      active-состояние по `usePathname`, пункты Приходы / Продукты / Дашборд / Настройки
- [ ] Grid-раскладка `sidebar + content`, адаптив с оверлеем на мобильном
- [ ] `Topbar` наполнить: время, счётчик активных сессий, переключатель языка, профиль
- [ ] Установить `formik`, `yup`
- [ ] Форма «Добавить приход»: title, date, description
- [ ] Форма «Добавить продукт»: все поля `Product`, включая цены USD/UAH и гарантию
- [ ] Ошибки под полями, дизейбл сабмита, обработка серверных ошибок
- [ ] Формы открываются в существующей модалке, подгружаются через `next/dynamic`
- [ ] Причесать таблицы и карточки, единые отступы и типографика

### Фаза 1.5 — i18n `0.5 дн`

- [ ] Установить `next-intl`, настроить провайдер в layout
- [ ] `messages/ru.json`, `en.json`, `uk.json`
- [ ] Выбор локали через cookie + переключатель в topbar
- [ ] Локализованные даты вместо хардкода `"ru-RU"` в `topMenu` и `formatDate`
- [ ] Перевести `"Все типы"` и остальные хардкод-строки
- [ ] Сообщения валидации Yup — через `t()`

---

## БЛОК 2 — пункты ТЗ (~7 дней)

### Фаза 2.1 — SSR `1.5 дн`

- [ ] Страницы `orders` и `products` → Server Components, данные тянутся на сервере
- [ ] Прокидывание cookie в серверные запросы
- [ ] Гидрация стора через `preloadedState`
- [ ] Интерактив (выбор прихода, удаление, фильтр) вынести в клиентские острова
- [ ] Форматирование дат зафиксировать на UTC — иначе hydration mismatch
- [ ] Часы рендерить только на клиенте

### Фаза 2.2 — JWT `1 дн`

- [ ] API: `POST /auth/login` → access 15m + refresh 7d, оба httpOnly + Secure + SameSite=Lax
- [ ] API: `POST /auth/refresh`, `POST /auth/logout`, `JwtAuthGuard`
- [ ] Фронт: страница логина на Formik + Yup
- [ ] Интерсептор `401 → refresh → retry` с дедупликацией параллельных рефрешей
- [ ] `middleware.ts` защищает роуты
- [ ] Socket авторизуется тем же токеном из handshake-cookie
- [ ] Logout: чистка cookie, стора и Web Storage

### Фаза 2.3 — GraphQL `1 дн`

- [ ] `@nestjs/graphql` code-first, схема Order / Product
- [ ] Запрос `orders { products { … } }` одним вызовом — решает реальный N+1
- [ ] Чтение через GraphQL, мутации через REST + axios
- [ ] `graphql-codegen` на фронте → типы в `services/generated/`
- [ ] `openapi-typescript` для REST-части контракта

### Фаза 2.4 — Charts и Web Workers `1 дн`

- [ ] Страница `/dashboard`
- [ ] Recharts через `dynamic(..., { ssr: false })`
- [ ] Графики: приходы по месяцам, распределение продуктов по типам, суммы USD/UAH
- [ ] Агрегацию вынести в Web Worker — UI не блокируется
- [ ] Хук `useStatsWorker` с корректным терминированием воркера

### Фаза 2.5 — PWA, Web Storage, оптимизация `1 дн`

- [ ] Serwist: manifest, service worker, офлайн-фолбэк, иконки, установка
- [ ] Web Storage через `useLocalStorage`: язык, состояние сайдбара,
      последний выбранный приход, офлайн-кэш последних данных
- [ ] `next/image` вместо строкового `photo`
- [ ] `React.memo` на карточках, мемоизация селекторов
- [ ] `@next/bundle-analyzer`, разбор бандла
- [ ] Lazy loading: `next/dynamic` для модалок, форм и чартов (skeleton уже есть)
- [ ] Виртуализация списка продуктов при большом объёме

### Фаза 2.6 — тесты `2 дн`

- [ ] Vitest + RTL + jsdom, конфиг под Next
- [ ] Unit: оба редьюсера, `formatDate`, `getOrderStats`, кастомные хуки, карточки
- [ ] MSW + интеграционные: загрузка страницы, удаление с пересчётом счётчиков,
      сабмит форм, обработка 4xx, refresh токена на 401
- [ ] Playwright + функциональные: логин → приходы → выбор → удаление
      с подтверждением → счётчики пересчитались
- [ ] E2E: смена языка; рост счётчика сессий в двух браузерных контекстах
- [ ] Coverage-порог
- [ ] GitHub Actions: lint + typecheck + test + e2e

### Фаза 2.7 — Docker и документация `0.5 дн`

- [x] Переписать Dockerfile и docker-compose под плоскую структуру, добавить
      `.dockerignore` (сделано досрочно вместе с выравниванием структуры)
- [ ] Compose на два сервиса, healthchecks, `.env.example`
- [ ] README: схема архитектуры, запуск, карта «пункт ТЗ → где реализован»
- [ ] Отдельный README в репозитории API

### Фаза 2.8 — Postgres `+1 дн, опционально`

Делать, только если API реально передаётся другой команде как рабочая заготовка.

- [ ] Prisma, схема, миграции
- [ ] `PostgresOrdersRepository` / `PostgresProductsRepository` — сервисы не трогаем
- [ ] Сид-скрипт из текущего мока
- [ ] Postgres в compose с healthcheck

---

## 5. Риски и подводные камни

1. **Гидрация дат (фаза 2.1).** `formatDate` и часы на сервере и клиенте дадут разный
   результат из-за таймзоны → hydration mismatch. Форматирование фиксируем на UTC,
   часы рендерим только на клиенте.
2. **Переделка из-за порядка работ.** Feedback-first означает, что клиентская загрузка
   из 1.3 переводится на SSR в 2.1. Чтобы это стоило часы, а не день: вызовы api сразу
   пишем изоморфными, `StoreProvider` с первого дня готовим под `preloadedState`.
3. **socket.io требует long-running сервер** — деплой на Vercel отпадает, нужен Docker/VPS.
4. **In-memory ломается при масштабировании.** Пока инстанс API один — всё корректно.
   Два и более — у каждого свой массив, счётчик сессий разъезжается. Лечение:
   фаза 2.8 + Redis-адаптер для socket.io. Для текущего скоупа не требуется.
5. **Два репозитория — нет атомарных изменений контракта.** Меняя API, сначала
   выкатываем бэк, потом перегенерируем типы на фронте. Локальная разработка требует
   поднять оба — закрывается общим compose.
6. **Cookie между разными портами в dev.** `SameSite=Lax` + разные origin фронта и API
   потребуют аккуратной настройки CORS `credentials` и, вероятно, прокси через Next
   rewrites на время разработки.

---

## 6. Открытые вопросы

- [ ] Postgres — нужен ли (фаза 2.8)? Решается тем, передаётся ли API другой команде.
- [ ] NestJS или Express для API? Принят NestJS; Express сэкономил бы ~1 день.
- [ ] Куда деплоим — VPS, Railway, Render? Влияет на фазу 2.7.

---

## 7. Оценка

| Блок                             | Срок                    |
| -------------------------------- | ----------------------- |
| Блок 1 — фидбек ревьюера         | ~5.5 дней               |
| Блок 2 — пункты ТЗ               | ~7 дней                 |
| **Итого**                        | **~12–13 рабочих дней** |
| Фаза 2.8 (Postgres, опционально) | +1 день                 |
